# Run: v0.6_dogfooding
# Date: 2026-05-11 05:27:00
# What changed: Started true dogfooding — VCM-OS server used to store its own dev memory.

## Server Startup

FastAPI server started on 127.0.0.1:8123 via `vcm_os.app.api`.
Endpoints tested: /health, /session/create, /events, /context/build

## Cross-Session Memory Test

Session 1 (sess_vcm_dev_001):
- Ingested 4 events:
  1. Decision: add rare-term rescue pass to pack builder
  2. Decision: use deterministic md5 hash for event_id
  3. Decision: create adversarial exact-symbol benchmark
  4. Error: TypeError in trace.py

Session 2 (sess_vcm_dev_002):
- Query: "What was the fix for the duplicate requirements bug?"
- Retrieved from Session 1's decisions:
  - "use deterministic md5 hash for event_id instead of Python hash()"
  - "add rare-term rescue pass to pack builder for missing protected keywords"
  - "create adversarial exact-symbol benchmark with sibling terms"

## Result

Cross-session project memory works on live server.
New session successfully retrieves decisions from previous session.
No contamination, no duplicates (clean DB), no stale decisions.

## Files Used

- vcm_os/app/api.py — FastAPI server
- vcm_os/storage/sqlite_store.py — persistent DB (data/vcm_os.db)
- vcm_os/memory/writer.py — event ingestion
- vcm_os/context/pack_builder.py — pack assembly
